apply from: "../commonHeader.gradle"

buildscript {
    apply from: "../commonHeader.gradle"  // for $kotlinVersion
    apply from: "../commonBuildScript.gradle"

    dependencies {
        classpath "com.android.experimental.built-in-kotlin:com.android.experimental.built-in-kotlin.gradle.plugin:${libs.versions.buildVersion.get()}"
    }
}
